<%= @category.name %>
<%= @category.url %>
<%= @category.title %>
<%= @category.keywords %>
<%= @category.disc %>


<%= link_to 'edit',   category_edit_path(@category.id)%>
<%= link_to 'delete',   category_delete_path(@category.id), method: :delete, data: { confirm: 'Delete?'}%>